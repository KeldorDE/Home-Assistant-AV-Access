services:
  homeassistant:
    image: ghcr.io/home-assistant/home-assistant:stable
    container_name: av-access-ha-dev
    restart: unless-stopped
    environment:
      - TZ=Europe/Berlin
    ports:
      - "8124:8123"
    volumes:
      - ./config:/config:rw
      - ../custom_components/av_access:/config/custom_components/av_access:rw
